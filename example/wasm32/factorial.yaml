name: "assert factorial(5) == 120"
limit: 500
memory_size: 0xFFFF
memory_mapped_io:
  0x80: [2]
  0x84: []
reports:
  - name: Steps
    slice: all
    view: |
      ---
      {pc}: {instruction:prev} {pc:label}
      {layout:hex}
